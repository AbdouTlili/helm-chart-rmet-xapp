apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rmet-xapp.fullname" . }}-test-connection"
  labels:
    {{- include "rmet-xapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rmet-xapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
